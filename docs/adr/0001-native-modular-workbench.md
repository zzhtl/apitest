# ADR-0001：保留原生模块化桌面架构

## 状态

已接受。

## 决策

继续使用 Rust、eframe/egui 和现有五个 crate 的模块化单体结构。应用层按工作台外壳、状态、服务和功能视图拆分，不引入 Web 运行时、微服务或动态插件 ABI。

## 取舍

原生方案可复用现有流式运行时、SQLite 和系统钥匙串，并保持较低资源占用；代价是复杂文本编辑和布局组件需要在 egui 生态内实现。
