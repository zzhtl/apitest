# ADR-0004：统一协议执行句柄

## 状态

已接受。

## 决策

所有协议通过 `ExecutorRegistry` 路由，并返回带事件流、取消令牌和可选交互命令通道的 `ExecutionHandle`。HTTP、GraphQL 和 SSE 可以共享执行器，WebSocket 与流式 gRPC 使用同一控制契约发送消息或结束输入。

## 取舍

统一句柄消除 UI 对协议实现的分支依赖，并支持一致的取消和生命周期隔离；代价是非交互协议需要明确拒绝不适用的控制命令。
