# ADR-0002：接口契约与请求用例分离

## 状态

已接受。

## 决策

`ApiDefinition` 保存结构化 `ApiContract`，`RequestCase` 保存可执行协议快照、变量、脚本、提取器和断言。运行时只消费由契约、用例和环境合成的 `ExecutionRequest`。

## 取舍

该模型允许设计、文档和调试共享一份契约，并支持多个用例；代价是保存与导入时必须维护定义和默认用例之间的显式关系。
